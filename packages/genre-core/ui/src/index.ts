/**
 * Genre UI module — Preact components for genre-specific forms and previews.
 * This package is only imported from the web app, never from server/domain code.
 */

export type { GenreUiModule } from "@track-forge/genre-core";
